<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="../public/stylesheets/style.css">
    <title>My cars</title>
</head>

    {{> header}}
    
<body>

    <div class="container">

        {{#if usuarios}}
            {{#each usuarios}}

            <div class="card" style="width: 18rem;">
                <div class="card-body">
                    <h5 class="card-title">{{username}}</h5>
                    <h6 class="card-subtitle mb-2 text-muted">{{fullname}}</h6>
                    <p class="card-text">{{email}}</p>
                    <a href="/users/deleteUser/{{id}}" class="btn btn-danger card-link">Borrar</a>
                    <a href="/users/editUser/{{id}}" class="btn btn-success card-link">Editar</a>
                    <a href="/auth/login/{{id}}" class="btn btn-warning card-link">Token</a>
                </div>
            </div>
            {{/each}}
        {{else}}
            <h1>NO HAY USUARIOS REGISTRADOS</h1>
        {{/if}}
    </div>
</body>
</html>